table! {
    coins (id) {
        id -> Varchar,
        name -> Varchar,
        symbol -> Varchar,
        rank -> Integer,
        available_supply -> Integer,
        total_supply -> Integer,
        max_supply -> Integer,
        last_updated -> Integer,
    }
}
